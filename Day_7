//create a regular expression to validate if the given input is a valid
//Indian mobile number or not
function validate(num) 
{
	var expr = /^\+91\s|^\+91|^0|^[0-9]{10}$/;
    let result = expr.test(num);
    return result;
}
